#include "global.hpp"
#include "Scanner.hpp"

class TaskF {
public:
    void solve(std::istream& inStream, std::ostream& outStream) {
        Scanner in(inStream);
        Writer out(outStream);
    }
};
